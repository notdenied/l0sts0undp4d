services:
  soundpad:
    build: .
    container_name: soundpad-app
    restart: unless-stopped
    ports:
      - "7331:7331"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
